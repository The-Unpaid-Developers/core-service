# Test configuration
spring.application.name=core-service-test
spring.data.mongodb.uri=mongodb://localhost:27017/solutions-test

# Disable MongoDB auto-configuration for pure unit tests
# spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.mongo.MongoAutoConfiguration

# Test specific settings
logging.level.org.springframework=WARN
logging.level.com.project.core_service=DEBUG
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.security.servlet.SecurityAutoConfiguration